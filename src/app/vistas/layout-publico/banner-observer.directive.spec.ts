import { BannerObserverDirective } from './banner-observer.directive';

describe('BannerObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerObserverDirective();
    expect(directive).toBeTruthy();
  });
});
